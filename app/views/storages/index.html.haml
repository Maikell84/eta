%h1 Lagerstände

%table.table
  %thead
    %tr
      %th Datum
      %th Wert
      %th Quelle
      %th

  %tbody
    - @storages.each do |storage|
      %tr
        %td= storage.created_at
        %td= storage.value
        %td= storage.source
        %td
          = link_to('', storage, class: 'fas fa-info-circle')
          = link_to('', edit_storage_path(storage), class: 'far fa-edit')
          = link_to('', storage, class: 'far fa-trash-alt', method: :delete, data: { confirm: 'Are you sure?' })

%br

= link_to 'New Storage', new_storage_path
